{
  "manifest_version": 1,
  "description": "Parity tracking for cueapi-action (the GitHub Action wrapping cueapi-cli) against the hosted CueAPI API and the cueapi-cli command surface. cueapi-action is a thin shell around `pip install cueapi && cueapi <command>`, so its parity is two-tier: (a) which cueapi-cli commands are exposed as Action inputs, (b) which CLI flag surface each exposed command covers. Drift becomes visible at audit time per the 3-layer parity discipline (PR template + this manifest + Backlog rows for each port).",
  "private_source_repo": "https://github.com/cueapi/cueapi",
  "private_source_paths": [
    "app/routers/",
    "cli/cueapi/cli.py (the CLI this action wraps)",
    ".github/cueapi-action.yml (private mirror)"
  ],
  "wraps_package": "cueapi (PyPI)",
  "wraps_package_repo": "https://github.com/cueapi/cueapi-cli",
  "wraps_package_manifest": "https://github.com/cueapi/cueapi-cli/blob/main/parity-manifest.json",
  "last_full_audit": "2026-05-07",
  "action_version_at_audit": "v1 (action.yml head)",
  "audit_methodology": "Walk every command in cueapi-cli's parity-manifest `commands_covered` and check whether action.yml exposes a matching `command:` input + the corresponding flag inputs. Drift goes in `commands_missing` (whole command not exposed) or `command_drift` (command exposed but flags incomplete).",
  "audit_cadence": "Per-cueapi-cli-release. When cueapi-cli ships a new minor (0.x.0), audit which new commands/flags need Action exposure and open a Backlog row per port.",

  "commands_exposed_via_action": {
    "create":     "cueapi create — covers --name, --cron, --at, --url, --method, --timezone, --payload, --description, --worker. Missing: --schedule-type alternates beyond cron/at, --headers, --retry-* config",
    "list":       "cueapi list — covers --status, --limit, --offset",
    "get":        "cueapi get <id>",
    "update":     "cueapi update <id> — covers --name, --cron, --url, --payload, --description. Missing: --status (use pause/resume), --headers, --transport",
    "delete":     "cueapi delete <id>",
    "bulk-delete":"cueapi bulk-delete <id>... --yes — POST /v1/cues/bulk-delete (cueapi #650, cli #46). Per-ID atomic, max 100. Action takes whitespace-separated `cue-ids` input and always passes --yes (CI has no interactive confirm). CLI sends X-Confirm-Destructive: true. Ported from Backlog row cmousydyn (originally mistitled 'messages'; corrected 2026-05-09).",
    "pause":      "cueapi pause <id>",
    "resume":     "cueapi resume <id>",
    "whoami":     "cueapi whoami",
    "usage":      "cueapi usage",
    "fire":       "cueapi fire <id> — covers --payload-override, --merge-strategy, --send-at (PR #618), --exit-criteria (PR #632 — whitespace-separated keys input), --idempotency-key (PR #683, this PR — body field on FireRequest, NOT a header).",
    "executions-list":          "cueapi executions list — covers --cue-id, --status, --limit, --offset, --outcome-state, --result-type, --has-evidence (boolean flag), --triggered-by. cueapi-cli PR #27 added the 4 filter flags upstream; this PR wires them all through.",
    "executions-list-claimable":"cueapi executions list-claimable — covers --task, --agent",
    "executions-get":           "cueapi executions get <id>",
    "executions-claim":         "cueapi executions claim <id> --worker-id <id>",
    "executions-claim-next":    "cueapi executions claim-next — covers --worker-id, --task",
    "executions-heartbeat":     "cueapi executions heartbeat <id> --worker-id <id>",
    "executions-report-outcome":"cueapi executions report-outcome <id> — covers --success/--failure, --external-id, --result-url, --summary",
    "agents-create":     "cueapi agents create — covers --slug, --display-name, --description, --webhook-url (PR #5, merged 2026-05-07)",
    "agents-list":       "cueapi agents list — covers --status, --online-only (mutex), --limit, --offset (PR #5, merged 2026-05-07)",
    "agents-get":        "cueapi agents get <ref> — also exposed as agents-describe verb alias per cueapi-cli #40 (PR #5, merged 2026-05-07)",
    "agents-describe":   "Verb alias for agents-get (PR #5, merged 2026-05-07)",
    "agents-update":     "cueapi agents update <ref> — covers --display-name, --description, --webhook-url, --clear-webhook-url sentinel (PR #5, merged 2026-05-07)",
    "agents-delete":     "cueapi agents delete <ref> — soft delete (PR #5, merged 2026-05-07)",
    "agents-webhook-secret-get":"cueapi agents webhook-secret get <ref> (PR #5, merged 2026-05-07)",
    "agents-webhook-secret-regenerate":"cueapi agents webhook-secret regenerate <ref> --yes — destructive (rotates webhook signing secret + drops old one immediately). The CLI sends X-Confirm-Destructive: true; action passes --yes since CI has no interactive confirm. Ported from Backlog row cmousycjv (Drift-audit deferred 2026-05-07; ported 2026-05-09).",
    "messages-send":"cueapi messages send — covers --from, --to, --body, --subject, --reply-to, --priority, --expects-reply, --reply-to-agent, --metadata, --idempotency-key, --send-at (PR #623), --mode (Surface 6 v2 delivery_mode; cueapi-cli #49 added it for parity with message-to, this PR wires through), --notify (PR #619 BCC-light; cueapi-cli #49 added it, this PR wires through whitespace-separated → repeated --notify flags). Ported via PR #6 (2026-05-07) + this PR for the --mode/--notify wire-through.",
    "messages-get":"cueapi messages get <msg_id> — single-message reveal (PR #6, merged 2026-05-07).",
    "messages-read":"cueapi messages read <msg_id> — mark message read (PR #6, merged 2026-05-07).",
    "messages-ack":"cueapi messages ack <msg_id> — acknowledge message (PR #6, merged 2026-05-07).",
    "message-to":"cueapi message-to <recipient> — top-level ergonomic alias for messaging primitive sends (cueapi-cli #40). Action reuses `to-agent` as the positional recipient (also accepts bare name / slug; CLI does roster lookup case-insensitive). Covers --from, --body, --subject, --reply-to, --priority, --expects-reply, --reply-to-agent, --metadata, --idempotency-key, --send-at (PR #623), --mode (Surface 6 v2; default-omit `auto`). Ported from Backlog rows cmousyb4a + cmousyqhy (Drift-audit deferred 2026-05-07; ported via this PR)."
  },

  "commands_missing_from_action": {
    "login": "cueapi login — interactive device-code flow, intentionally not in Action surface. CI passes CUEAPI_API_KEY via secret instead.",
    "logout": "cueapi logout — local-state only, no API call. Not relevant in Action context.",
    "quickstart": "cueapi quickstart — interactive walkthrough, not relevant.",
    "key regenerate": "POST /v1/auth/key/regenerate — destructive, intentionally gated behind manual flow. NOT-PORTED-INTENTIONAL.",
    "key webhook-secret get": "GET /v1/auth/webhook-secret — could be exposed for `outputs:` in CI workflows that need to verify webhook signatures. NEEDS-PORT.",
    "key webhook-secret regenerate": "POST /v1/auth/webhook-secret/regenerate — destructive. NOT-PORTED-INTENTIONAL.",
    "agents roster": "GET /v1/agents/roster (cueapi #630) — NEEDS-PORT in cueapi-cli + cueapi-action; cueapi-cli upstream port pending.",
    "agents presence": "GET /v1/agents/{ref}/presence (cueapi #662) — NEEDS-PORT in cueapi-cli + cueapi-action; cueapi-cli upstream port pending.",
    "agents inbox": "GET /v1/agents/{ref}/inbox — NEEDS-PORT (cueapi-cli has it via #28; Action wrap pending).",
    "agents sent": "GET /v1/agents/{ref}/sent — NEEDS-PORT (cueapi-cli has it via #28; Action wrap pending).",
    "executions replay": "POST /v1/executions/{id}/replay (cueapi-cli PR #31) — NEEDS-PORT. In-flight as PR #8.",
    "executions verification-pending": "POST /v1/executions/{id}/verification-pending (cueapi-cli PR #31) — NEEDS-PORT. In-flight as PR #8.",
    "executions verify": "POST /v1/executions/{id}/verify (cueapi-cli PR #31) — NEEDS-PORT. In-flight as PR #8.",
    "messages delete": "DELETE /v1/messages/{id} — bounded by cueapi-cli upstream (no per-id delete in cueapi-cli yet)."
  },

  "command_drift": {
    "create": [
      "Missing flag --headers (custom HTTP headers for callback). cueapi-cli supports it; Action does not."
    ],
    "messages-send": [],
    "fire": [],
    "executions-list": []
  },

  "post_2026_05_04_cueapi_prs_not_yet_in_action": [
    "#630 — GET /v1/agents/roster (Agent Directory v0)",
    "#650 — POST /v1/cues/bulk-delete (cueapi-action exposes via `bulk-delete` command; was originally mis-noted as 'messages bulk delete' on this list; corrected 2026-05-09)",
    "#662 — GET /v1/agents/{ref}/presence (cheap-poll variant)",
    "#664 — POST /v1/executions/{id}/live-claim (Live-claim attestation, cmotigtnx)"
  ],

  "process_observations": [
    "cueapi-action wraps cueapi-cli, so its drift surface is bounded by cueapi-cli's coverage. When cueapi-cli is behind on a feature, cueapi-action is necessarily behind too. Manifest refresh cadence should chain: cueapi-cli refreshes after each cueapi/cueapi merge → cueapi-action refreshes after each cueapi-cli release.",
    "Several Action drift items resolve automatically as cueapi-cli ships. For drift that requires a NEW Action input (e.g. --send-at on fire), the Action needs explicit input declaration in action.yml + the env-var injection block.",
    "First audit (this manifest) covers ~17 commands exposed and ~24 commands missing. Of the 24 missing, 21 are NEEDS-PORT and 3 are NOT-PORTED-INTENTIONAL (interactive or destructive). The Surface 6 messaging primitive (agents + messages) is the largest single un-ported feature group."
  ]
}
